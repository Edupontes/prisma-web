export default function Placeholder({ title }) {
  return (
    <div>
      <h2>{title}</h2>
      <p style={{ opacity: 0.7 }}>
        (tela em construção — vamos detalhar quando chegar na etapa deste
        módulo)
      </p>
    </div>
  );
}
